<% provide(:title, @user.name) %>

<div class="row">
  <aside>
    <section class="user_info">
      <h1>
        <%= gravatar_for @user, 80 %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
  <div>
    <%= if @user.microposts.any? %>
    <% end %>
  </div>
</div>
